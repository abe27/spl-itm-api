package services

func Logger(userID, title, description string) {
	// var log models.SystemLogger
	// log.UserID = &userID
	// log.Title = title
	// log.Description = description
	// log.IsSuccess = true
	// if err := configs.Store.Create(&log); err != nil {
	// 	panic(err)
	// }
}
